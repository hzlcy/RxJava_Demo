<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="scut.carson_ho.rxjava_operators">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".rxjava.CreateActivity" />
        <activity android:name=".rxjava.ChangeActivity" />
        <activity android:name=".rxjava.FiltrationActivity" />
        <activity android:name=".rxjava.MergeActivity" />
        <activity android:name=".rxjava.FunctionActivity" />
        <activity android:name=".rxjava.UniteAndJudgeActivity" />
        <activity android:name=".rxjava.AntiShakeActivity" />
        <activity android:name=".rxjava.SearchActivity" />


        <activity android:name=".MainActivity" />
        <activity android:name=".MainActivity3"/>
    </application>

</manifest>